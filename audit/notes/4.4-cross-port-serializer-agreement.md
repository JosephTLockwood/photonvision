# Check 4.4 — Cross-port serializer agreement (Java / C++ / Python)

- Iteration: 2026-05-16T12:16-04:00
- Audit root commit: `0e834f0851584237b0dd0913381b52fe6110dd08`
- Files read at that SHA:
  - `photon-targeting/src/generated/main/java/org/photonvision/struct/PhotonPipelineMetadataSerde.java`
  - `photon-targeting/src/generated/main/native/cpp/photon/serde/PhotonPipelineMetadataSerde.cpp`
  - `photon-targeting/src/generated/main/native/include/photon/serde/PhotonPipelineMetadataSerde.h`
  - `photon-lib/py/photonlibpy/generated/PhotonPipelineMetadataSerde.py`
  - `photon-targeting/src/main/native/include/photon/dataflow/structures/Packet.h` (lines 1-145)
  - `photon-targeting/src/main/java/org/photonvision/common/dataflow/structures/Packet.java` (decodeLong, from 4.1)
  - `photon-lib/py/photonlibpy/packet.py` (encodeLong, from 4.1)

## Schema metadata agreement

All three ports report:

| port | MESSAGE_VERSION (md5)              | MESSAGE_FORMAT                                                                                              |
|------|-----------------------------------|-------------------------------------------------------------------------------------------------------------|
| Java | `ac0a45f686457856fb30af77699ea356` | `"int64 sequenceID;int64 captureTimestampMicros;int64 publishTimestampMicros;int64 timeSinceLastPong;"` |
| C++  | `ac0a45f686457856fb30af77699ea356` | (same string)                                                                                               |
| Python | `ac0a45f686457856fb30af77699ea356` | (same string)                                                                                               |

Sources:
- Java: [Serde.java:45-47](photon-targeting/src/generated/main/java/org/photonvision/struct/PhotonPipelineMetadataSerde.java#L45-L47).
- C++: [Serde.h:43-49](photon-targeting/src/generated/main/native/include/photon/serde/PhotonPipelineMetadataSerde.h#L43-L49).
- Python: [Serde.py:40-42](photon-lib/py/photonlibpy/generated/PhotonPipelineMetadataSerde.py#L40-L42).

The MESSAGE_VERSION hash is exchanged on connect; a mismatch causes deserialization to
reject the packet. **Strong defensive coverage** against silent renames or unit changes.

## Field order agreement (pack/unpack)

All three ports pack/unpack the same four `int64` fields in the same order:

1. `sequenceID`
2. `captureTimestampMicros`
3. `publishTimestampMicros`
4. `timeSinceLastPong`

Verified at:
- Java: [Serde.java:60-69, 76-86](photon-targeting/src/generated/main/java/org/photonvision/struct/PhotonPipelineMetadataSerde.java#L60-L86).
- C++: [Serde.cpp:33-46](photon-targeting/src/generated/main/native/cpp/photon/serde/PhotonPipelineMetadataSerde.cpp#L33-L46).
- Python: [Serde.py:48-77](photon-lib/py/photonlibpy/generated/PhotonPipelineMetadataSerde.py#L48-L77).

## Byte order

- **Java** [Packet.java:315-330](photon-targeting/src/main/java/org/photonvision/common/dataflow/structures/Packet.java#L315-L330):
  `decodeLong` shifts `<<0, 8, 16, 24, 32, 40, 48, 56` → **little-endian**.
- **C++** [Packet.h:92-111](photon-targeting/src/main/native/include/photon/dataflow/structures/Packet.h#L92-L111):
  `Pack<T>` delegates to `wpi::util::PackStruct<T>`. WPILib's struct library specifies
  **little-endian** packing for all integral types (per the wpilib struct spec).
- **Python** [packet.py:241+](photon-lib/py/photonlibpy/packet.py):
  Uses `struct.pack("<q", value)`. The `<` prefix is **little-endian**.

All three ports produce 8-byte little-endian int64. ✓

## Auto-generation guarantee

All three serdes carry the comment:

    // THIS FILE WAS AUTO-GENERATED BY ./photon-serde/generate_messages.py. DO NOT MODIFY

So the per-port code is derived from a single source-of-truth (`messages.yaml`) by a
single generator (`generate_messages.py`). Skew between ports would require the generator
to emit inconsistent output, which is unlikely.

## Verdict

**Clean.** Cross-port wire-protocol agreement is enforced via:
1. Single source of truth (`messages.yaml`) + auto-generated per-port serdes.
2. MD5 schema-hash handshake on connect (rejects schema-drifted peers).
3. Identical field order, identical types, identical byte-order convention.

The historical risk vectors for serde divergence (hand-rolled per-port serializers, no
schema-version check) don't apply here.

No FINDINGS.md entry.

## Stage 4 closure

All four sub-checks closed:
- 4.1: captureTimestampMicros unit/encoding — clean.
- 4.2: publishTimestampMicros unit/encoding — clean, but surfaced **DIVERGENCE** in
  Java↔C++/Python `getTimestampSeconds` (filed) + **CORRECTION** to 1.1 (Python comment).
- 4.3: sequenceID / timeSinceLastPong units — clean.
- 4.4: cross-port serializer agreement — clean.

Stage 4 net: 0 findings of its own; one DIVERGENCE escalated from Stage 6 work surfaced
during 4.2; one correction to 1.1.
